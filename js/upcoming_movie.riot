<upcoming_movie>

    <section class="section">
        <div class="container">
            <h1 class="title">Les films à venir</h1>
        </div>
        <div each="{item in this.state.upcoming[this.state.upcoming['page']]}" class="column is-one-third">
            <div class="card">
                <div class="card-image">
                    <figure class="image">
                        <img src="https://image.tmdb.org/t/p/original/{item['poster_path']}" alt="Placeholder image">
                    </figure>
                </div>

                <div class="card-content">
                    <div class="media">
                        <div class="media-content">
                            <p class="title is-4">{item['title']}</p>
                            <p class="subtitle is-6">{item['release_date']}</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <div class="page-navigation">
        <button class="button is-primary"
            onclick="{() => this.getUpcomingMovies(this.state.upcoming['page'] - 1)}">Previous</button>
        <p>Page {this.state.upcoming['page']} of {this.state.upcoming['total_pages']}</p>
        <button class="button is-primary"
            onclick="{() => this.getUpcomingMovies(this.state.upcoming['page'] + 1)}">Next</button>
    </div>

    <script>
        export default function upcoming_movie() {
            return {
                async onBeforeMount(props, state) {
                    this.state = {
                        upcoming: [],
                        page_index: 1
                    }
                    await this.getUpcomingMovies(this.state.page_index)
                },

                async getUpcomingMovies(page_index) {
                    let api = apiModel()
                    let upcoming = await api.getUpcomingMovies(page_index)
                    this.state.upcoming['total_pages'] = upcoming['total_pages']
                    this.state.upcoming['page'] = page_index
                    this.state.upcoming[this.state.upcoming['page']] = upcoming['results']
                    this.update()

                }
            }
        }
    </script>

</upcoming_movie>