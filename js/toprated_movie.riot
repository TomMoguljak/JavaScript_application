<toprated_movie>

    <section class="section">
        <div class="container">
            <h1 class="title">Les films les mieux notés</h1>
        </div>
        <div each="{item in this.state.toprated[this.state.toprated['page']]}" class="column is-one-third">
            <div class="card">
                <div class="card-image">
                    <figure class="image">
                        <img src="https://image.tmdb.org/t/p/original/{item['poster_path']}" alt="Placeholder image">
                    </figure>
                </div>

                <div class="card-content">
                    <div class="media">
                        <div class="media-content">
                            <p class="title is-4">{item['title']}</p>
                            <p class="subtitle is-6">{item['release_date']}</p>
                            <p class="subtitle is-6">{item['vote_average']}</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <div class="page-navigation">
        <button class="button is-primary"
            onclick="{() => this.getTopRatedMovies(this.state.toprated['page'] - 1)}">Previous</button>
        <p>Page {this.state.toprated['page']} of {this.state.toprated['total_pages']}</p>
        <button class="button is-primary"
            onclick="{() => this.getTopRatedMovies(this.state.toprated['page'] + 1)}">Next</button>
    </div>

    <script>
        export default function toprated_movie() {
            return {
                async onBeforeMount(props, state) {
                    this.state = {
                        toprated: [],
                        page_index: 1
                    }
                    await this.getTopRatedMovies(this.state.page_index)
                },

                async getTopRatedMovies(page_index) {
                    let api = apiModel()
                    let toprated = await api.getTopRatedMovies(page_index)
                    this.state.toprated['total_pages'] = toprated['total_pages']
                    this.state.toprated['page'] = page_index
                    this.state.toprated[this.state.toprated['page']] = toprated['results']
                    this.update()
                }
            }
        }
    </script>

</toprated_movie>